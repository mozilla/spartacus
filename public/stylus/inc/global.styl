* {
    box-sizing(border-box);
}

.visually-hidden {
    visually-hidden();
}

@media $extra-small-max-width {
    .S-visually-hidden {
        visually-hidden();
    }
}

.hidden {
    display: none;
}

.hidden.show {
    display: block;
}

.no-wrap {
    white-space: nowrap;
}
